# trained adapters and their checkpoints: hundreds of MB per arm, and the
# arms are reproducible from the pinned configs and seeds
runs/arms/
runs/logs/
runs/state/
runs/sheets/
# the full contact-sheet gallery is ~670MB of renderings; runs/pairs carries a
# representative set covering every axis, and contact_sheets.py regenerates
# the rest from runs/maps
data/
third_party/
__pycache__/
*.pyc
.venv/
