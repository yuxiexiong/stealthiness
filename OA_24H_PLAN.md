# OA 最终执行计划：复用作者模型，双 H20 约 24 小时

## Material Passport

- Origin Skill / Mode：academic-research-suite / experiment-agent / plan。
- 日期：2026-09-09；版本：reuse-24h-v3-soft-budget。
- Verification Status：计划与对应代码已更新；CPU 验证见 [VERIFICATION.md](VERIFICATION.md)。H20 合成算子已测；真实模型阶段和本矩阵尚未运行。
- **本文件是本轮唯一执行范围。**[原完整协议](EXPERIMENT_PLAN.md)留作后续研究设计，[旧 24h 版本](reports/oa_gpu_benchmark_2026-09-09/OA_24H_PLAN_v1_WITH_REUSE_CORRECTION.md)仅供追溯。

## 1. 本轮要回答什么

先只做 OA §4.2：查清训练和评测的主要成本、隐蔽约束增加的计算、公开终点在固定检查下的表现，选出下一轮最值得验证的一项预算。

**完整终点直接用作者权重；不新跑 3000 步，也没有 1500 步降级训练。**新训练仅用于短段成本归因。OA-2 的收敛轨迹、首次联合达标和完整 OA-3 的低预算质量比较都后移。Cordyceps、BadVision 本轮不启动。

24h 是双 H20 实验作业的目标量级，约 48 分配卡时，**不是硬截止**。接近 24h 时优先完成正在收尾的关键配对，不自动杀进程；若真实计时预计明显超过一天，则减少可选项目与轻量参照，不扩大矩阵。安装、下载、排队和外部评分 API 等待另记；作业中的模型加载、初始化、CPU 拟合持卡等待、搬运、保存和失败耗时均计入。不能用“GPU 利用率为零”扣掉等待。

## 2. 复用的模型与代码

共同基座：`meta-llama/Meta-Llama-3-8B-Instruct`。三个作者 LoRA 各约 130 MB，共用一份基座缓存；仅在各 GPU 作业中加载所需状态，不额外保存三份合并后的完整模型。

| 状态 | 来源 | 本轮用途 |
| --- | --- | --- |
| M0：干净基座 | 上述 Meta 基座，无 adapter | 行为参照、Gaussian/input 轻量检测 |
| M1：普通后门 | [no-obfuscation](https://huggingface.co/Mechanistic-Anomaly-Detection/llama3-deployment-backdoor-model-no-obfuscation)，revision `03926d2f6d9c50b29d9341470bf080821721d4da` | 主终点之一，完整检测面板 |
| M2：MAD | [obfuscate-mad](https://huggingface.co/Mechanistic-Anomaly-Detection/llama3-deployment-backdoor-obfuscate-mad)，revision `0fd4da751a70d0820791363649d97747038416fa` | 行为、正常输出保留、Gaussian/input 轻量检测 |
| M3：MAD＋probes | [obfuscate-mad-probes](https://huggingface.co/Mechanistic-Anomaly-Detection/llama3-deployment-backdoor-obfuscate-mad-probes)，revision `68b259fcf842f1eb9fee6ecc47002388a727fd57` | 主要研究终点，完整检测面板 |

复用 OA commit `9c1f4b1a721fa3c43d00b8605ee7db4c84a55942` 和 cupbearer commit `1fd0c4fcf5e0b7a3e9c7024fa4119c9643853ce6` 的训练器、检测器、生成与评分函数。继续用已验证的 TED 拟合后缓存 GPU 驻留路径；不改其 FP64、近邻、排名或 PCA 定义，不预支其他尚未验证的缓存优化收益。

公开权重是终点资源，不包含已确认的 §4.2 中间训练轨迹、优化器、最终防守检测器或完整逐样本评分包。每个终点仍需独立拟合与校准检测器。资源证据及其他模型的归属限制见 [复用审计](OA_PRETRAINED_REUSE_AUDIT.md)。

## 3. 固定实验矩阵

| 编号 | 实验 | 固定规模 | 能得到的证据 |
| --- | --- | --- | --- |
| R1 | M1、M3 两个发布终点的完整面板 | 各自 Gaussian、VAE、Beatrix、TED、Linear、MLP；全部 32 层；input / generation 两位置 | 普通后门与隐蔽终点的行为、检测差异；各检测器的实际成本与显存 |
| R2 | M0、M2 的轻量参照 | Gaussian/input，32 层；相同测试提示上的行为；M2 对基座的正常输出保留 | 基座及 MAD 变体的粗略位置，不能当完整隐蔽性检查 |
| T1 | 普通后门与 MAD＋probes 各一条短训练成本参照 | 两条匹配的 256 微步短轨迹，梯度累积 4，即各 64 次更新；128 / 200 / 256 微步观察；同基座、原训练数据流、每流 batch 2、seed 0 | 预热和启用约束后的成本分布；当前实现的隐蔽约束额外负担 |
| T2，有明确热点时优先 | 由 T1 选出的一个高占比预算轴，匹配短段成本验证 | T1 证据支持后再选轴；原预算和一个降低档位；在额外 MAD 轻量参照之前安排；不做多轴搜索 | 该轴是否实际节省计算；不提供低预算质量合格证据 |

### R1 / R2 的数据与质量检查

- 两个主终点各用 trusted clean 512 拟合、calibration clean 512 定阈值；held-out clean / harmful / backdoor 各 128。Linear、MLP 另用正例 128、负例 128 拟合；负例可取 trusted 的子集，不能混入校准或测试。
- 保留现有 `prepare-data --n-train 512 --n-cal 512 --n-eval 128 --n-probe 128` 的确定性取样规则：测试取各作者 split 末尾 128 条；保存源 revision、数据 fingerprint、清单与提示 hash。四个状态共用同一清单，先冻结再跑，不按结果换子集。这是固定子集诊断，不能声称随机代表全部任务或已证实作者从未见过每条记录。
- 拟合、校准、测试提示按本轮清单检查互不重叠；监督 probe 的拟合来源另记。检测器在每个状态分别重拟合。主终点每个位置实际评分为 `512 + 128 + 128 = 768` 条。
- 生成沿用作者函数，请求 batch 8，输出上限 200 token；不靠截短输出省时。两个主终点各生成 `512 + 512 + 3×128 = 1408` 条，共 2816 条。轻量参照只生成三个行为测试组，共 `2×384 = 768` 条；其输入检测只读提示，入口为 `--generation-scope behavior`。作者函数的 OOM 后减 batch 逻辑保留，实际调用批次与失败记录单列；不能把请求 batch 当成实测 batch。
- 最终 OOD detector batch 1，避免现有 generation hook 的 left-padding 偏移问题；32 层与两位置完整保留。VAE 1 epoch；监督 probes 10 epochs、batch 64；这是冻结的审计配置，并非已经核实的全部作者历史 run 配置。
- TED generation 最多取 8 token 的既有配置保持；这不是将模型回答截成 8 token。最大输入长度 4096，记录触及截断的样本，不能将截断样本的结果外推为完整长文本检查。
- 分别报告 ASR、正常/有害提示上的误触发、正常输出 KL 保留代理，以及各检测器 AUROC、经验 1% FPR 校准阈值下的检测率和 held-out FPR。层级结果保留，不平均掉单项失败。M0 与自身 KL 按定义为零，不冒充一项独立能力测试。
- 行为继续使用作者 StrongREJECT 评分方法；GPU 窗口先交付生成文本。外部评分模型、费用及调用单列；未经授权不自动调用付费 API。若未评分，则 ASR / FTR 明确待定，不能改用关键词评分补成结果。正常输出保留不是广泛任务正确率。
- 本轮只做单 seed、单子集的描述性比较。128 条测试中一次事件约 0.781 个百分点；512 条校准能给出经验阈值，不能据此认证真实 FPR ≤ 1% 或细小的非劣差异。绝对质量门槛尚未冻结时只报告数值，不输出“合格提效”。

### T1 的训练边界

新建短训练 adapter，保留作者训练结构与损失；已知 LoRA 配置对齐发布权重的 rank 16、alpha 16、dropout 0.05。训练约束层仍为 `[4,8,12,16,20,24,28]`，MAD＋probes 每层仍为 48 个 probes；原数据池、学习率 `1e-4`、损失权重 `3/6/3`、约束更新方式不为省时间而修改。训练数据不缩成评测用的 512 条。

两个短段要求相同的初始 LoRA 权重与批次顺序，记录各流样本 hash、有效 token 和 padding 数。代码已接入独立且持续推进的 detector RNG 流、固定 adapter 初始化和各数据流独立采样生成器；真实运行后仍须核对初始参数与有序曝光 hash，不能只凭相同 seed 宣称匹配成功。

沿用现有 3000 微步参考配置的学习率日程，在 256 微步停止：保留上游 `CosineAnnealingLR(T_max=3000)` 及其按参数更新推进的语义，不压缩为短训长度。3000 是本地参考配置，不声称是所有发布权重已确认的历史步数。“日程长度”与“停止位置”在薄封装中分开，旧接口单独传短 `--microsteps` 不满足此要求。

128 微步是首次观察点，不默认视为已经稳定。为避免自动续训、恢复状态与两卡停止协调，默认两条短轨迹都跑到 256，并保留 128 / 200 / 256 的匹配前缀观察；不按质量选择停止点。

预热/初始化、前 100 次 detector 调用与启用约束后的阶段分开；在第 100 微步之后采集 8 步 profiler，并逐步记录不含 profile 的计时、实际曝光与长度。比较相同长度的后续计时窗口，波动仍大时明确报告“尚无可靠稳态”，不自动追加训练。核对实际 detector 调用计数，未跨过约束启用点不能当稳定段。测模型前后向、参考计算、激活、detector 更新、伪逆、probe 约束、保存和峰值显存；共享前向单列，嵌套 profiler 时间不重复相加。

短段结束的模型不做主终点评测，不从作者终点继续训练来伪装早期训练，也不拿短段乘总步数当精确的作者训练成本。T1 配置对齐了已知字段，仍只能代表当前实现的短段成本。

T2 只在 T1 中定位最高成本的可控因素后选择一次，选择依据是成本占比，不是质量成绩。有明确热点时优先于额外 MAD 轻量参照；没有证据时不预写或自动发起一个假定有效的减预算实验。若无法在约一天的总规模内完成匹配参照和降低档位，则整体延期；单边结果不算预算比较。无论是否执行，本轮完整 OA-3 的“减少预算后质量是否仍达标”都延期。

## 4. 双卡排程与成本依据

以下为**可调整的时间分配，不是各项已测 ETA 或停止计时器**。两卡跑独立作业，不依赖双卡训练加速；主终点不依赖 T1 的训练产物，短训完成后即可切换。两卡先完成 T1 和 R1 的核心队列；T1 输出后据证据安排 T2，再决定轻量参照范围，不让固定时段锁死信息价值。

| 窗口 | GPU0 | GPU1 | 两卡合计预算 |
| --- | --- | --- | ---: |
| 0–2h | 普通后门 T1，含加载与短预检 | MAD＋probes T1，含加载与短预检 | 4 卡时 |
| 2–20h | 作者 M1 完整面板与生成 | 作者 M3 完整面板与生成 | 36 卡时 |
| 20–22h | 有明确热点则做 T2，否则 M0 轻量参照 | 有明确热点则做 T2，否则 M2 轻量参照 | 4 卡时 |
| 22–24h 附近 | 补充参照、波动余量、保存与整理 | 同左 | 4 卡时 |
| **目标量级** | **约 24h** | **约 24h** | **约 48 卡时** |

每段提前完成就顺序前移，不刻意空等。加载、生成、拟合等均包含在各段内；共用 CPU/磁盘时记录实际争用，两份单卡时间不能直接作为并发实测。为保存和整理留余量；不为凑满 24h 而追加实验，也不在 24h 整点中断即将完成的关键比较。

目前 H20 合成算子测量支持的**单个主终点**成本条件外推：

| 已测部分 | H20 卡时/终点 |
| --- | ---: |
| Beatrix 两位置更新、评分和收尾 | 6.216 |
| TED 两位置 GPU 评分 | 1.812 |
| TED CPU 拟合持卡等待 | 0.400 |
| VAE | 0.052 |
| Gaussian 求逆部分 | 0.016 |
| **小计** | **8.497** |

两个主终点已测部分合计约 **16.99 卡时**，若主面板安排合计 36 卡时，则还可容纳约 19.01 卡时尚未测的模型前向、生成、监督 probes、加载和其他开销；其余约 12 卡时用于 T1、T2 或 R2 及余量。36/48 是评测的预算占比，不是实测成本占比。不能把 16.99 当成整个实验成本，也不能把额外研究评测的耗时当成攻击构造成本。

两个主面板还约有 86,540 次样本前向及 2816 条生成（生成不计入该前向数）；不同 padding / 序列长度不能统一乘一条短提示的速度。早期测量应覆盖真实长度、完整 32 层显存，以及 Beatrix/TED 等重步骤，不能只测便宜 detector。

旧完整矩阵约 136.38 卡时的已测部分是另一份工作量，已不适用于本轮。本轮节省主要来自直接复用端点、减少完整评测状态和测试样本，并非取得了同质量的算法提速。**约 24h 有可行性，但完整 ETA 尚未闭合；真实模型短段后按剩余工作量更新排程。**测量证据见 [GPU_ETA.md](GPU_ETA.md) 与 [算子报告](reports/oa_gpu_benchmark_2026-09-09/REPORT.md)。

## 5. 软预算管理与明确牺牲

1. T1 约 2h/卡是暂定时间额度；实际停止按 256 微步短训预算，不按 2h 整点自动中断。保留实际完成步数与阶段，主终点仍加载作者权重，绝不改成新训的残缺终点。单个成本参照缺失时，不能报告完整的训练增量比较。
2. 根据真实阶段速度更新各卡剩余时间；明显超出约一天的规模时，先减少未启动的轻量参照及没有明确热点支撑的 T2。保留 M1/M3 关键配对和有效短段计时优先，不能缩层或静默换检测器来标记其完成。
3. R2 未完成就注明 M0 或 M2 的缺项，不拿另一状态填补；若两个主终点只有一方完成，也只能报告单方结果和已成对完成的项目。
4. 运行入口按约 24h 记录预算提醒和实际进度，不自动杀死核心作业。若预期大幅超时，应在阶段边界依据耗时缩减剩余范围，而非无限延长。保存已完成的检测器结果、未执行列表、实际卡时与失败记录。缺任何必要检查的联合质量标作未判定，不按有利分数选择结论。OOM 或异常不自动换小模型、改精度或稀释检查条件。

本轮明确牺牲：**完整训练过程、首次联合达标/安全早停结论、完整低预算质量比较、M0/M2 的昂贵完整面板、512 条 held-out 的统计精度、多 seed 与跨任务泛化。**保留两个关键发布终点的六类/32 层/两位置检查，以及作者的行为评分定义。

## 6. 代码状态与开跑条件

已有：作者训练/生成/检测/评分适配、发布 adapter 的 tokenizer 回退加载、128 测试量配置、Gaussian/input 轻模式、阶段计量、TED 缓存 GPU 路径及 H20 算子回归。最新 CPU 检查与边界见 [验证记录](VERIFICATION.md)，不能替代真实模型验证。

本版本已接通：

1. `train --cost-probe --microsteps 256 --schedule-microsteps 3000 --lora-rank 16 --lora-alpha 16 --lora-dropout 0.05`：原训练器自然在 256 微步结束，128/200/256 观察；各流身份和计时落盘。
2. `--base-model` 显式加载共同本地基座；省略 `--snapshot` 运行干净基座。轻量生成记录真实生成的三个组，保留作者 probe 示范；完整检测拒绝部分生成产物。模型/config/tokenizer 字节身份与生成数据、参数、seed 绑定，旧无生成范围凭据的文件不自动升级。
3. [oa_run.py](experiments/oa_run.py) 默认只预览；显式 `--execute` 后用现有计量器组织两个顺序核心队列。约 24h 只记录软预算，不强杀；跨阶段人工等待不计入 GPU 实验时间。核心阶段为 T1+R1，T2 先据证据选轴，再显式执行所需 controls；不自动下载、评分或上传。作者端点的权重与完整 adapter 配置另按核验值检查。
4. 启动说明见 [experiments/OA.md](experiments/OA.md)。真实模型加载、两位置 mask、32 层 detector 峰值显存和完整阶段耗时仍须在准备好基座后验证，相关运行成本计入实验预算。

基座可用性仍是前置条件：上次服务器检查没有找到指定基座，现有 HF 账号请求返回 403。三个 adapter 可访问，不等于基座已经可用。解决合法基座访问或提供匹配缓存后再开始 24h 时钟；依赖安装/下载单列，不混入 GPU 实验 ETA。此文档整理没有启动新的 GPU 作业。

## 7. 交付与下一轮决策

计划输出目录：`runs/oa-reuse-24h-<run-id>/`。保留冻结清单、四状态来源、生成文本、逐样本/逐层检测分数与阈值、短段 trace、显存与成本账、收尾时覆盖清单。最终中文报告单列：已测事实、条件外推、缺项和下一步建议。

交付一张诊断表：训练高成本环节、评测高成本环节、隐蔽约束额外成本、公开终点的质量、可复用边界、下一轮预算轴及预期能影响的成本比例。若 T2 未完成，“预算敏感性”只登记候选；“最先失守的质量”统一留待完整预算实验，不能从终点差异推断。

成本主要在训练通用前后向，则后续验证训练步数/曝光；主要在隐蔽约束，则验证对应探针/层/更新预算；主要在评测，则先研究验证流程的复用，并将其与攻击构造成本区分。只有完成配对低预算训练及完整质量确认后，才讨论同质量提效倍数。
