# 三篇基线的效率诊断实验规划

**本文件保存原完整研究协议，不是本轮作业清单。当前执行范围以 [OA_24H_PLAN.md](OA_24H_PLAN.md) 为准：复用作者模型，双 H20 约 24 小时软预算，两个完整终点面板、短段成本诊断及按信息价值安排的单轴成本验证/轻量参照。**下文完整 OA-2 轨迹和完整 OA-3 预算质量比较均后移；不新跑完整训练。Cordyceps、BadVision 暂不启动。旧快筛、旧完整矩阵的预算均不能当作本轮 ETA。

## 状态与材料

- 日期：2026-09-09；版本：v1。
- Origin Skill：academic-research-suite / experiment-agent，plan。
- Verification Status：论文已审阅；实验适配与 CPU 检查完成；OA 已执行 H20 合成算子测试，完整模型实验仍未执行，见 [验证记录](VERIFICATION.md)。
- 论文基准：本仓库 `dbf5d1c4360a6c1002893d628ed27404209c06d1` 的三份 PDF，见 [论文目录](papers/README.md)。
- 用户已同意先诊断再决定方法、不提前排序；后续“测试并给 GPU ETA”已授权服务器测试。付费生成、上传模型未执行。
- 沿用 [效率审计协议](../poison-r0/reports/poison_efficiency_audit_2026-09-06/PROTOCOL.md)。本轮不完成现行 SOTA 认证。

## 1. 研究问题与成功条件

找出原方法成本分布、可能冗余的预算、与隐蔽性有关的额外计算，以及减少预算时首先不能满足的质量要求。分别回答“成本是否值得降低”和“是否已有可降低的证据”，不合并成排名。

主要产出是每篇一张诊断表，至少包括：

| 字段 | 要求 |
| --- | --- |
| 基线资格 | 原始配置、攻击者权限、原版是否满足冻结的质量要求 |
| 成本 | 分阶段墙钟、GPU 小时、峰值显存、数据与调用量 |
| 隐蔽性增量 | 当前实现中与检测约束有关的计算；共享计算单列 |
| 预算敏感性 | 预算减少带来的实际节省和各质量项变化 |
| 首先失守的质量 | 毒效、误触发、逐任务正常能力、逐检测器结果 |
| 复用边界 | 触发器、数据、模型、目标和评测之间哪些可复用 |
| 效率指标 | 可研究的指标、证据等级、限制与下一步 |

一次短段或单 seed 只支持筛选。低预算候选只有在独立确认中保持约定质量，才属于效率空间证据。组件关闭仅用于归因，不直接成为合格方法。

## 2. 原完整研究的实施顺序与验证（本轮删减见最终计划）

1. 先完成本规划，再编写最小适配层；验证：每个探针有上游入口、成本边界、质量检查与缺口状态。
2. 固定上游版本并检查数据、权重、依赖与路径；验证：预检或 dry-run，不执行下载后的任意安装脚本。
3. 编写复用作者训练/评测函数的适配代码；验证：CPU 上检查数据单位、命令构造、日志、成本计算与质量判定，GPU 部分明确标为未测。
4. 在指定 GPU 上先做短段成本诊断，再形成完整参考轨迹；验证：原版行为与独立评测一致。H20 算子测试已获授权并执行；完整模型短训因基座缓存缺失和 HF 403 尚未运行。
5. 冻结合格参照、质量规则和诊断轴，再运行低预算比较；候选与参考至少各 3 个独立 seeds 确认。

## 3. 联合质量规则

### 3.1 先定义规则，再比较预算

每项质量分别保存方向、单位、绝对门槛或相对原版容忍幅度。不用单一加权分数，也不默认三个方法共享相同阈值。

首次运行先测原版与 clean reference。根据应用要求形成冻结的规则文件，之后不得根据候选成绩放宽规则。没有规则、缺少必要指标、非有限值或原版不合格，均不能输出“合格提效”。不得将缺失指标当作零。

同时保留两种问题的区别：

- 是否满足预先约定的绝对要求；
- 是否在约定容忍幅度内保持原版质量。

规则尚未确定时可以计时、输出完整质量曲线，但不自动给出成功标签。原版在全部检查点都不满足要求时，记为“预算内未联合达标”，不删除失败检测器或改变正常任务集合。

### 3.2 原文已知限制

- OA：锁定 §4.2 的训练控制权限。输入端和生成端检测分开；generation TED 的结果不能被其他检测器平均掩盖。见原文图 10、附录 B.3。
- Cordyceps：第一轮主场景为 BCC，UCC 单独登记。保留作者 CA，同时核验数值对应字段、禁止明文的具体对象以及检查范围是否含 reasoning。Table 8 的逐任务正常能力必须保存，不能只用平均值。
- BadVision：保留实际 LVLM 输出、正常任务、误触发和 DECREE；Sim-T/Sim-B 仅是诊断指标。原 ASR 是目标主要概念出现率，不是完整细节正确率。DECREE 原阈值为 P_L1 < 0.1 判为后门；是否仅需越过阈值，须在规则中明确。

### 3.3 检查点与统计

OA/B 的初始检查点为约 10%、25%、50%、100% 的实际优化器更新数；若要对齐作者保存周期，记录取整后的真实位置。中间状态是完整轨迹前缀，不等于重新缩短学习率调度后的训练结果。

同一数据划分下使用配对评测；检测器拟合、阈值校准、预算选择验证和最终确认数据各有用途。OA 每个模型检查点都重新拟合最终防守检测器，不能沿用旧权重。

稀疏检查点只能确定首次达标区间。单次达标与连续后续检查点均达标分别记录；“隐蔽性最后达标”只识别最后满足的约束，不直接构成额外成本的因果解释。候选确认应报告 seeds、样本数、逐项差异与不确定性；点估计通过不等于统计非劣结论。

## 4. 成本账本

| 阶段 | 必须计入的内容 |
| --- | --- |
| 数据构造 | 有效生成、失败/重试、筛选、输入/输出 tokens、等待、独立语义单位、成品条目 |
| 构造与训练 | 触发优化、模型训练、参考计算、隐蔽组件、保存；预热与稳定阶段分开 |
| 方法自身验证 | 如果方法依靠多次评测选择检查点或配置，计入其方法成本 |
| 外部评测 | 固定下游评测、检测器拟合/校准/检测、清除与迁移验证 |
| 研究开销 | profiler、额外诊断、消融与调试单列，不伪装成方法固有成本 |

分别报告方法构造成本和完整安全评估流程成本。标明攻击者、模型训练者、评测者各自承担的部分。API 成本和 GPU 成本不任意换算成一个分数。

独占 GPU 下，分配 GPU 小时 = 墙钟小时 × 分配卡数；利用率曲线另报，不能称作实际 FLOPs。显存的采样峰值与框架记录的峰值不是同一口径。主计时以同步后的完整阶段为准；细分 profiler 的自耗时不能在有嵌套/重叠时直接相加。

数据同时记录原始记录数、独立内容数、提交条目数、token 数、优化器更新、微批与重复曝光。数据补齐并不意味着 token 计算量相等。

## 5. OA 探针

| 探针 | 最小实验 | 解释与边界 |
| --- | --- | --- |
| OA-1 | 原 §4.2 配置的预热/稳定短段，拆模型计算、参考计算、激活、检测器更新、矩阵计算、probe、保存与验证；普通后门仅作成本参照 | KL 保持正常输出；activation retention 限制表征漂移。共享前向单列，不能把全部参考成本归给其中一项 |
| OA-2 | 一条完整轨迹保留上述检查点，记录累计成本及所有质量项；每次重新训练最终 detector | 对齐优化器更新与学习率状态；不使用训练 detector 分数代替新 detector |
| OA-3 | 仅在 OA-1 后选择一个高占比轴，以原预算和约一半预算起步；进一步档位根据诊断信息决定 | 不预定 probe/层/更新数哪项冗余；减少数量时保持损失归一化与其他权重可比，最终 detector 集合保持不变 |

48 probes 已有分布变化方面的动机（附录 B.2），减少后应重点检验新数据上重拟合 detector 的结果。只在模型可直接修改的权限下讨论这组成本。

### 5.1 完整轨迹方案的历史执行顺序（本轮不执行）

以下保留版本演变证据，不能作为当前运行指令。终点复用、LoRA 对齐、短段停止与当前质量范围均由 [最终计划](OA_24H_PLAN.md)规定。

1. 固定可追溯的训练配置、模型/数据来源及质量规则；当前源码默认 LoRA 是 rank64/alpha128/dropout0，作者发布 adapter 是16/16/0.05。配置差异尚未解释前，不能把它们混成同配置成本或质量对照；也不为省钱静默换成较小 rank。
2. 原预算完整参考轨迹仍为当前代码的3000微步、750次参数更新。开启已有局部 profiler，收集100微步之后的8步算子归因，随后继续同一完整轨迹；不为这份原配置另跑一条128/400步短训。普通后门的独立成本参照仍保留，不能用已训练权重反推其训练耗时。
3. 在300、752、1500、3000微步保存的检查点上执行完整约定评测，即约10%、25%、50%、100%的更新位置。数据规模、32层、六类检测器及input/generation两位置保持原配置，每个模型状态重新拟合最终检测器；毒效、误触发和正常能力要求也保留。`--screening`不用于这些结果。
4. 根据OA-1实测，只选一个高占比预算轴开展OA-3。保留原预算与低预算的配对比较，不提前把probe数量减半认作有效方案；候选与参考采用相同完整评测。若选中的轴是训练更新数，应先利用完整参考轨迹已有前缀，并明确其调度与重新训练的区别。
5. 有候选后再按原规划完成独立确认。两个GPU执行独立作业；候选需要等成本归因确定配置，不能为了并行提前选择预算轴。

完整轨迹带profiler的总墙钟必须保留，并标作包含研究观测开销。当前记录并未测出profiler的纯扰动，不能把trace中嵌套事件简单相加再从总时长扣除；非profile稳定区间只能另报吞吐估计。保存、初始化和必要验证不从总账中隐去。

### 5.2 完整轨迹方案的历史节省设想与当时状态

| 节省方式 | 状态与边界 |
| --- | --- |
| TED可信激活驻留GPU | 在原CPU拟合结束后迁移最终张量，保持dtype、原近邻/排名/PCA与评分顺序。提供CPU缓存原路径作回归；不减少层、邻居或样本。完整配置input约0.5GiB、generation最多4GiB额外缓存，实际显存仍需H20验证 |
| OA-1与OA-2共用原预算训练 | 现有`--profile`与完整训练入口已支持；减少重复原版短训，不减少参考轨迹或普通后门成本参照 |
| 复用作者已训练端点 | 公开资源记录见筛选文档。仅省掉为端点验收而单独重训的作业；不能提供原历史成本、中间轨迹或可精确续训状态，也不自动代替同配置参考 |
| 复用已有生成/评测产物 | 仅限同一基座、adapter权重和配置、tokenizer、数据划分、生成/检测配置及seed完全匹配。当前adapter权重hash本身不足以证明这些条件；本轮不新增按单一hash自动跳过评测的缓存 |
| 两卡独立排程 | 参考训练与独立成本参照可以并行；已保存检查点可在空闲卡评测。减少等待不等于减少总卡时 |
| TED排名复用、跨检测器/层共享激活 | 留待驻留GPU版本的计时后决定。尚未实现或数值验证，不计入当前节省；必须先核对逐层分数、阈值和最终判定 |

以上表格为当时状态；随后已完成 TED 原路径/驻留路径的合成算子配对检查，见 [实测记录](GPU_ETA.md)。当前仍未运行完整模型质量实验，不优化训练中的`pinv`更新频率或损失函数。当前 24h 范围及待接通代码见 [最终计划](OA_24H_PLAN.md)。

## 6. Cordyceps 探针

| 探针 | 最小实验 | 解释与边界 |
| --- | --- | --- |
| C-1 | 先审计现成数据和调用日志；有官方生成入口时再对固定小批样本计量 Phase 1、Phase 2、格式化、筛选与失败调用 | 不从成品样本反推历史调用数。若生成源码/日志缺失，明确标记，不能用自行重写流程冒充原方法成本 |
| C-2 | BCC 原数据、减少独立示范并用干净记录补足总条目、减少独立示范但重复补足污染条目三种对照；保持原 victim 训练程序 | 初筛一个中等减少档位，不预定 100→10；锁定独立目标/锚点/记录的统计单位。重复对照只证明信息量方面的可能性，不自动降低训练成本 |
| C-3 | 原 BCC 与直接检索成本参照；分别统计新目标输入构造、训练、victim 推理及验证，整理跨模型数据复用 | 原文已有未见目标泛化；不得将每个新目标都按重新训练计费。直接检索不是满足隐蔽要求的替代方法 |

原 BCC 100/1000 与 100/10000 的对照改变总训练量，不是独立示范数扫描。作者公开训练器优先复用 LLaMA-Factory；不改变 victim 的采样/调度/损失来声称同权限提效。

原附录 reasoning 权重消融属于不同数据条件，权重归零本身不证明删除 token 或生成费用；不得拿来充当低预算完整结果。

## 7. BadVision 探针

| 探针 | 最小实验 | 解释与边界 |
| --- | --- | --- |
| B-1 | 触发器优化、encoder 训练、TF 的 PGD 与参考计算、数据处理、保存、LVLM 评测和 DECREE 分别计时 | 保留原 TO/TF/RF 消融为已有证据；关闭 TF 的时差仅为当前实现增量 |
| B-2 | 从触发器构造开始累计成本，完整 encoder 轨迹保留检查点，测实际输出、正常任务、FAR、DECREE | 不能用 Sim-T/Sim-B 代替下游质量；原预处理、扰动限制和检测配置保持一致 |
| B-3 | 先共用同一触发器，对原规模与较小图像集建立更新数匹配的两条轨迹 | 第一轮仅归因 encoder 阶段。只有产生候选后才追加减少数据的触发器全流程，支持端到端少数据主张 |

原论文设置为触发器 10 epochs、CLIP encoder 30 epochs、EVA encoder 50 epochs、batch size 4。CLI 默认值与论文设置不能混用；第一轮先 CLIP/LLaVA，EVA/MiniGPT 是后续确认，避免初始全组合。

## 8. 最小运行矩阵与两卡安排

第一轮不做跨模型、跨目标、全部预算的笛卡尔积。每篇先一个作者设置、一个训练 seed 和固定诊断数据；选择的目标/子集 seed 必须留在运行记录中。

| 阶段 | 最小工作量 | 是否已运行 |
| --- | --- | --- |
| 本地预检 | 固定上游代码、数据单位、命令、结果处理 | 已完成 CPU 范围；GPU 环境未就绪 |
| 短段诊断 | OA 原版/普通成本参照；B 原版/组件归因；C 数据审计和可用原入口 | 否 |
| 完整参考 | OA 原版轨迹；BCC 原数据训练；B 触发器 + 原数据轨迹 | 否 |
| 初筛 | OA 一个预算轴；C 一个低独立样本档及重复对照；B 一条小图像轨迹 | 否 |
| 独立确认 | 有信息价值的候选及参照补至至少 3 seeds；固定完整评测 | 否 |

两张 H20 暂按独占、每项单卡可容纳来排队，能否容纳须预检确认。优先两张卡分别处理独立方法；任何使用两卡的任务独占该窗口，不能以 wall-clock 减半推定 GPU 小时也减半。共享服务器则记录其他负载，无法归因的短段不用于正式成本对比。

## 9. 代码复用与当前已知缺口

核心训练、投毒方法与检测器优先调用官方实现；本仓库只增加必要的配置/数据准备、计量、检查点接线和结果核验。采用每篇独立环境，避免合并冲突的 torch/transformers 依赖。上游版本和最小修补单独登记，不把未经核对的默认参数当论文配置。

- OA：官方路径依赖与安装脚本不一致；已固定作者 cupbearer 分支并接入本地路径，关闭默认 Hub 上传和在线 W&B。原代码未发现明确许可证时不把源文件整体复制进本仓库。
- Cordyceps：当前公开树中尚未找到 Phase 1/2 生成脚本与调用账本；部分 run.sh 路径不存在。优先复用实际存在的训练器、配置和 BCC 评分入口。
- BadVision：已绕过主入口配置字段错误，用最小观察点保留独立检查点，接入官方 DECREE 优化循环。原论文的 DECREE 适配配置未公开，本轮适配须先经干净参照与可检出对照校准；未校准时禁止声称联合评测完成。

任何仍缺源码、数据、权重或检测器的探针都列出具体缺项。CPU 测试通过只代表适配逻辑验证，不能称 GPU 复现成功。

## 10. ETA 的定义与更新

ETA 分开报告环境/模型准备、短段诊断、参考轨迹、预算初筛、完整评测和确认。优先依据同硬件短段的稳定耗时与完整配置推算：

当前排期预算、工作量及材料缺口见 [ETA.md](ETA.md)，不能将该预算当成 H20 实测。

`预计阶段时间 = 一次性时间 + 剩余更新数 × 稳定每更新耗时 + 已知保存/评测时间`。

两卡总工期由实际调度的较长队列决定，并受共享依赖和显存限制影响。BadVision 论文 8 小时只作外部参考，不直接换算为 H20 时间。没有 H20 实测或模型/数据到位时，给明确假设下的区间或公式，不承诺精确完成时刻。API 等待与缺失材料另列，不计成确定的 GPU 时间。

## 11. 决策规则

- 高占比开销 + 低预算联合质量稳定：进入方法设计。
- 简单减预算失败：记录限制质量，保留有价值问题。
- 早停/合理配置已经显著降本：纳入强对照。
- 仅改善小步骤而完整收益低：关闭该局部轴。
- 原版不达标、检测缺失或口径不同：不能给“合格提效”结论。
- 历史成本不可恢复：报告未知，只用前瞻性计量形成新证据。

阶段占比 f、局部加速 s 时，完整加速上限为 `1 / ((1-f) + f/s)`。占比须来自测量；5–10 倍仅为选题目标，不是预期结果承诺。
