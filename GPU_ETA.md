# OA：H20 实测账与原完整矩阵 ETA

更新：2026-09-09。**当前执行范围已经改为 [复用作者模型＋双 H20 约 24h](OA_24H_PLAN.md)。两个主终点在缩减测试量后的已校准检测部分合计约 16.99 卡时，模型计算等仍未测；24h 是软预算，不是硬截止或已验证完成 ETA。**

本页下文保存合成算子实测及原完整矩阵的条件外推。原矩阵已校准部分约 136.4 H20 卡时、双卡理想均分约 68.2 小时（2.84 天），**已经不是当前工作量**。未执行完整模型短训，基座访问/缓存仍待解决。新计划取消完整训练，不能继续将其成本加入本轮预算。

之前的 5.3 天、旧三篇 487 卡时等数字不能沿用为本轮实测结论。[旧场景计算](reports/oa_gpu_benchmark_2026-09-09/GPU_ETA_BEFORE_MEASUREMENT.md)保留供追溯。

## 已测结果

直接调用固定作者源码，输入为合成激活/协方差，没有加载语言模型。H20、PyTorch 2.3.1、CUDA 12.1；每进程 8 个 CPU 计算线程。通常预热一次、正式两次；CUDA 阶段前后同步。

| 测量单位 | input | generation，8 token |
| --- | ---: | ---: |
| TED 完整 32 层，CPU 缓存评分/样本（一次对照） | 20.279 秒 | 137.004 秒 |
| TED 完整 32 层，GPU 驻留评分/样本（稳定复测均值） | 1.698 秒 | 6.795 秒 |
| TED CPU 拟合核外推/位置/状态 | 335 秒 | 1,105 秒 |
| Beatrix，单层含四个 powers，更新/样本 | 0.2846 秒 | 0.2780 秒 |
| Beatrix，单层含四个 powers，评分/样本 | 0.2707 秒 | 0.2647 秒 |
| VAE，单层训练更新/样本 | 0.00489 秒 | 0.00484 秒 |
| VAE，单层评分/样本 | 0.000541 秒 | 0.000621 秒 |

4096×4096 FP64 对称伪逆：**0.9121 秒/次**。使用合成正定矩阵，真实协方差谱可能影响耗时。

TED 同一拟合状态、同一查询的 CPU/GPU 缓存对照：input、generation 均逐层完全一致，最大绝对差为 0。既测了小规模真实 TED 拟合，也测了完整 32 层评分形状；后者 PCA 来自合成排名，未执行完整真实数据拟合。**验证的是缓存迁移的算子分数，不是模型毒效、正常能力或真实检测阈值。**

## 对原计划的条件外推

两条训练暂按各 3,000 微步原预算计；两个位置、32 层，每位置 512 条拟合和 1,536 条评分；八个 checkpoint 加干净参照共九个状态。OA-3 预算轴尚未选定；若最终选更新数并能复用参考轨迹前缀，应重新计数。

| 已校准部分 | H20 卡时 |
| --- | ---: |
| 训练中的 42,000 次伪逆 | 10.64 |
| 最终 Gaussian 的 576 次伪逆 | 0.15 |
| TED GPU 驻留评分本体 | 32.61 |
| TED CPU 拟合，在持卡进程中串行等待 | 3.60 |
| Beatrix 更新、收尾与评分，单层外推到 32 层 | 88.84 |
| VAE 更新和评分，单层外推到 32 层 | 0.54 |
| **已校准部分小计** | **136.38** |
| **双卡理想均分的小计** | **68.19 小时** |

只计实验作业，不计安装、下载、工程调试、排队或付费 API 等待。CPU 拟合在持有模型/GPU 的进程中串行执行，因此保留在分配卡时里。Beatrix 保留原 Gram、上三角索引和统计操作，本轮没有改算法或砍预算。

## 原完整矩阵的 ETA 缺项（非本轮执行预算）

`完整卡时 = 136.38 + 训练中除伪逆外的工作 + 672,822 次评测样本前向 + 23,040 条生成 + Gaussian 其余计算 + 监督 probe 训练/评分 + 初始化/数据/必要搬运/保存等开销`。

上述前向计数不含生成 prefill/decode；监督 probe 整组 padding 与 OOD batch 1 的长度不同。不能把未测项填零，或统一乘一次短 prompt 前向的耗时。最终两卡 ETA 还受 checkpoint 依赖、任务大小、CPU 争用和实际显存影响，不能保证简单除二。单层/合成数据外推不是统计区间或严格下界。

**实际阻碍：**已检查的服务器缓存没有 `meta-llama/Meta-Llama-3-8B-Instruct`；现有 HF 账号请求 config、权重索引及首个分片均返回 HTTP 403 / GatedRepoError。没有绕过权限或换模型。[模型可用性摘要](runs/oa-gpu-bench-20260909/model-access.json)已保存。

模型可用后，测原训练预热与稳定微步、代表性完整评测阶段、真实序列/生成长度，才能闭合完整 ETA。准备耗时不加入 GPU ETA，但模型缺失会阻止测量。当前不承诺“几天全部跑完”。

## 证据

- [测试详情、公式和复跑方式](reports/oa_gpu_benchmark_2026-09-09/REPORT.md)
- [机器可读成本小计](runs/oa-gpu-bench-20260909/eta_components.json)、[环境记录](runs/oa-gpu-bench-20260909/environment.json)
- [伪逆/TED 测试脚本](experiments/oa_gpu_bench.py)、[Beatrix/VAE 测试脚本](experiments/oa_detector_bench.py)

原始 JSON/log 位于本地 `runs/oa-gpu-bench-20260909/` 和服务器 `/workspace/stealthiness-oa-20260909/runs/gpu-bench/`。`runs/` 被 Git 忽略，不会自动随代码发布。
