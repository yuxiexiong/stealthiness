# Grond 首轮：配对结果复核与历史成本审计

- 版本：`grond-evidence-v1`，2026-09-09。
- Origin Skill / Mode：Academic Research Suite experiment-agent / plan；实现使用 Ponytail full。
- 状态：计划已写；本轮不把作者结果、软件测试和实际模型复评混为一谈。
- 范围：分析已经存在的模型、预测、官方结果 CSV 和运行记录。不生成触发器，不训练或优化后门，不搜索训练预算。

## 1. 要回答的问题

1. 原文表 9 中，无防御攻击成功率相近、清除后表现差异明显的现象，能否从配对原始产物复核？
2. 对应结果的完整构造、检查点选择与研究评估成本能恢复多少？缺失在哪里？

这轮不能证明 ABI 每次操作都必要、找到最低训练预算、确认最早联合达标，或证明十倍提效。表 9 是质量消融；表 15 是架构比较，均不能替代成本记录。

## 2. 来源与资源边界

- [原文 v3](https://arxiv.org/html/2501.05928v3)，重点为 §4.1、表 9、表 15。
- [官方仓库](https://github.com/xiaoyunxxy/parameter_backdoor)，固定版本 `c52b0f7be660e2c940095da4a50f3d9dacce4b54`。
- 本次官方树盘点发现的 `results/upgd_*.pth` 是扰动产物，不是受害模型权重；完整树中未发现配对受害模型、历史结果 CSV 或计时日志。该结论限于这个仓库版本，不等于作者没有其他材料。
- 此提交早于论文 v3，不能直接宣称它完整对应 v3 的所有实验；本轮只确认输出格式和指标口径。
- 源码使用现有 `experiments/sources.py` 固定版本独立获取。原代码不复制进本仓库，也不由分析工具执行。

## 3. GQ：固定配对结果复核

只覆盖原文表 9 的 ResNet-18 / CIFAR-10 对照，保留 UPGD 与 UPGD+ABI 两组，以及无防御、CLP、FT-SAM 三个观察条件。不是六个新训练任务。

| 组别 | 条件 | 作者 BA（%） | 作者 ASR（%） |
| --- | --- | ---: | ---: |
| UPGD | 无防御 | 93.86 | 98.61 |
| UPGD+ABI | 无防御 | 93.43 | 98.04 |
| UPGD | CLP | 91.15 | 3.97 |
| UPGD+ABI | CLP | 93.29 | 87.89 |
| UPGD | FT-SAM | 91.80 | 51.77 |
| UPGD+ABI | FT-SAM | 92.02 | 80.07 |

以上只作作者报告参照，不写入“本轮实测”字段。首轮不另定成功率阈值，也不自动宣布复现成功。

### 输入优先级

1. 官方 `train.py::eval_model` 导出的 CSV：列名为 `model,Test Natural,Test POI`，比例单位为百分数。精确选择一条记录；多条匹配时拒绝猜测“最新”或“最好”的一条。
2. 已有逐样本预测 CSV：`sample_id,split,y_true,y_pred`，其中 `split` 是 `clean` 或 `triggered`。`y_true` 必须保留原始类别，不能写被替换的目标类别。样本 ID 指向原始测试图像，不允许同一 split 内重复。
3. 对应配置 JSON、所评估权重和预测来源记录：保留路径和 SHA-256。声明相同的训练配置只支持“配置记录一致”，不能证明两条轨迹实际配对；权重哈希也不能证明 CSV 确实由它产生。

预测来源记录须声明 `checkpoint_sha256`、`predictions_sha256`、`target_class`、`variant`、`defense`；存在配置文件时还须声明 `config_sha256`。代码核对内容哈希与条件标签；它不是运行来源的密码学证明。

配置 JSON 的比较字段为 `dataset`（固定 `CIFAR10`）、`architecture`（固定 `ResNet18`）、`target_class`、`seed`、`train_split_sha256`、`poison_ids_sha256`、`init_checkpoint_sha256`、`preparation_artifact_sha256`、`updates`、`batch_size`、`optimizer`、`training_schedule`、`preprocessing`、`defense_settings`。它们描述两个组应匹配的条件；ABI 组别由清单的 `variant` 表示。需人工核验配置记录的含义、完整性与真实执行情况；相同 JSON 不是实际执行相同的证明。

只有来源哈希相符、配置字段齐全且相同、样本身份一致、两组权重并非同一文件内容时，才输出“声明条件匹配”的百分点差值。缺项或不匹配会保留逐项指标，但不会给出配对通过结论。只有汇总 CSV 时，无法核验其分母与逐样本配对。

### 评分与比较

- BA：全部 clean 行中预测等于真实类别的比例。
- ASR：triggered 行中，原始类别不是目标类别的样本，预测为目标类别的比例。不额外过滤原模型本来就分类错误的样本。
- 同时报分子、分母、排除的目标类别样本数。完整 CIFAR-10 测试应有 10,000 个 clean 样本和 9,000 个非目标类测试样本；子集结果明确标记为子集。
- 先检查两组逐样本记录的 ID、split、原始标签一致，再报告 BA/ASR 的百分点差异。只分析固定样本的点估计，不把样本数当独立模型数，不输出跨 seed 显著性结论。
- CLP 与 FT-SAM 行用于抗清除证据；不能叫“检测率”。缺少检测实验时，不给完整隐蔽性通过结论。
- 缺预测时，只能导入官方 CSV 的报告值；缺 CSV 和预测时，指标为未知。数据错误、重复样本、含糊的 CSV 行、非法数值和不匹配的哈希必须报错。

## 4. GC：历史成本账

直接读取已有 `experiments/measure.py` 生成的 `run.json`，不另造计时器。原始记录也可以人工整理成同样的字段，但须在清单注明来源；这类整理记录不能冒称历史运行器原生输出。

每条记录有 `stage`（辅助模型、准备、目标训练、检查点选择、评估）、`scope`（UPGD、UPGD+ABI 或共享）、`record` 路径和原始来源说明。固定的阶段槽位没有记录时保持未知。

- 构造与检查点选择成本同研究评估成本分开。
- 所有已记录尝试均保留，失败或中断不从账中消失；缺失时长不记为零。
- GPU 小时采用运行记录定义的分配卡时；核对时长、卡数与记录的卡时是否一致。未记录卡数时仍为未知。
- 去重使用原始文件路径和内容哈希，防止共享记录或其副本重复计费。
- 已知记录只输出已知小计。清单无法证明所有历史尝试都已收集，所以完整历史成本默认仍未知。
- 不将并行作业的时长相加称为端到端墙钟。GPU 卡时小计也不等同 FLOPs；重叠分配和记录来源须复核。
- ABI 包含在目标训练中；没有独立 profiler 记录时，它的单独耗时未知。不能把目标训练整段时间都记到 ABI，也不能把父计时与内部子计时相加。

阶段是否重复、共享是否实际成立、失败尝试是否漏报需要原始证据。代码列出缺项，不推算作者未报告的 GPU 小时。

## 5. 最小实现与执行顺序

1. 完成本计划。
2. 在 `experiments/upstreams.json` 登记官方版本，复用 `sources.py` 做只读源码准备/版本检查。
3. 增加一个标准库分析入口 `experiments/grond_audit.py` 和一份空数据清单 `experiments/grond_manifest.example.json`。清单中的 `null` 是材料缺失，不是零结果。
4. 复用 `measure.py` 记录分析进程的真实状态。输出 `report.json`、`report.md`；不覆盖已有目录。
5. 用小型合成记录测试解析、分母、配对、哈希和成本去重；运行空清单，确认缺项会如实暴露。合成测试与研究数据分开保存。

```bash
python3.12 experiments/sources.py grond --check
python3.12 -m experiments.grond_audit \
  --manifest experiments/grond_manifest.example.json \
  --out runs/grond-readiness
python3.12 -m unittest discover -s tests -p 'test_grond_audit.py' -v
```

默认命令只分析本地记录，不下载数据，不加载 pickle/PyTorch 权重，不运行上游训练或清除脚本。固定权重的模型前向复评需要先取得权重、对应测试数据与配置，本轮缺这些材料，不能宣称已经实现或完成 GPU 复现。

## 6. 交付与验收

- 计划、最小分析代码、清单和可运行测试。
- 六个条件各自的证据级别：缺失 / 导入报告值 / 从预测重新计算。
- 配对条件与来源核验结果、与作者报告的差值；这些不自动升级成复现认证。
- 已知成本小计、缺失阶段、失败尝试和重复记录问题。
- 首次真实 readiness 报告及软件测试记录。没有实际模型推理时 GPU 复评耗时与 ETA 均未知，不填“双卡 24 小时能完成”。

完成软件验收不等于完成 Grond 实验。拿到所缺配对产物后，本入口可以直接分析其结果；不能从作者终点表倒推出训练轨迹或低预算有效性。
