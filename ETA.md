# 实验 ETA 与排期假设

**历史排期，非当前OA排期。**最新要求为先OA、恢复原完整探针，仅节省GPU开销；当前范围见 [EXPERIMENT_PLAN.md 第5节](EXPERIMENT_PLAN.md#5-oa-探针)，计时状态见 [GPU_ETA.md](GPU_ETA.md)。先前半天快筛预算已撤回；下文三篇合计的5–10天也不能套用于当前OA单项。

此段为 H20 测量前的历史状态。2026-09-09 已完成 OA 合成算子计时，完整模型短训受基座访问权限阻塞；当前结果见 [GPU_ETA.md](GPU_ETA.md)。

**历史算量更新：**当时含 TED 重复搬运的三篇中档估算为双卡 250–270 小时，现保存在 [旧场景计算](reports/oa_gpu_benchmark_2026-09-09/GPU_ETA_BEFORE_MEASUREMENT.md)。下文 5–10 天也属于此前粗排，不能当作当前 OA 的 ETA。

## 当前可给出的预期

**先为首轮诊断预留 5–10 个自然日；有价值方向的多 seed 确认再预留 1–2 周。** 这是低置信度的项目排期预算，不是根据 H20 实测得到的完成时间，也不是保证所有方向都能在预算内形成结论。

起算条件是：两张 H20 可以持续独占；每个单卡任务容纳原配置；模型、数据、依赖和必要的评测规则到位。环境从零准备另预留 1–3 天；访问权限、缺失材料、共享服务器排队和人工标注等待不含在这个区间中。若原配置不能单卡容纳，应先修订运行安排并登记配置差异，不能靠静默缩短序列或改变 batch 来兑现 ETA。

| 交付节点 | 起算后排期预算 | 覆盖范围 |
| --- | --- | --- |
| 第一份成本诊断 | 第 1–2 天 | 三篇可运行入口的短段、预热/稳定成本、显存与测量开销；不含提效结论 |
| 首轮诊断表 | 第 5–10 天 | 单 seed 原版参考、一个预算轴/中等减少档、相关检查点与固定评测；材料缺口原样保留 |
| 候选确认 | 再加 1–2 周 | 只对筛选出的方向把参考与候选补至至少 3 seeds，并完整评测；不预定三篇全部确认 |

上述窗口包含环境接线后的调试和串行依赖余量。正式启动后，先以同硬件短段更新本文件；如果检测器或长输出推理占主导，应延长窗口，不减少检查强度。

## 已确定的计算工作量

首轮并非只跑三次训练：参考加预算初筛至少有 **7 条训练轨迹**，另有短段成本参照、干净参照、下游生成和检测。

| 方法 | 参考与初筛工作量 | 单条原版轨迹的代码口径 | 额外评测 |
| --- | --- | --- | --- |
| OA | 原版 + 一个待成本诊断选出的预算配置，共 2 条 | 3,000 微步 / 梯度累积 4 = 750 次优化器更新；这是固定作者脚本配置，不是声称论文统一采用此预算 | 每条约 4 个检查点，逐点重新拟合全部冻结检测器；生成、毒效评分与正常能力另计 |
| Cordyceps | original / reduced / repeat，共 3 条 | 每条 1,000 条记录 × 5 epochs；若预处理保留全部记录，单卡 batch12 / 累积4 约 105 更新，最终以 Trainer 实际 max_steps 为准 | 至少各自完整终点评测；BCC 公布测试为 101 条，默认每条最多生成 16,384 tokens；ONION 与正常能力另计 |
| BadVision | 原规模 + 小图像集匹配更新，共 2 条 | 原版 trigger 12,500 batch + encoder 37,500 更新；小图像轨迹复用同一个 trigger，再做 37,500 更新 | 每条约 4 个 encoder 检查点；实际 LLaVA 输出、正常任务、误触发与独立 DECREE 反演另计 |

OA 的微步包含多个数据流和检测器计算，不能与另两篇的一次更新直接比较。BadVision encoder 更新内部还包含 PGD；不能用普通 CLIP 微调吞吐替代。Cordyceps 的等条目数也不保证等 token 或等耗时。

OA 当前监督 detector 适配在每层复用四种 probe 组合的激活，但不同层仍重新前向。默认每个检查点约有 `(128 + 128 + 512 + 512 + 512) × 32 = 57,344` 次样本前向；每条 4 点轨迹约 229,376 次，尚不含 OOD 检测器与生成。StrongREJECT 每个检查点还有 1,536 次正常/有害/后门评分调用，4 点合计 6,144 次，当前串行。仅作换算示例：若实测平均每次评分 10 秒，一条轨迹的评分等待约 17.1 小时；10 秒是假设，不是服务实测值。评分等待与 GPU 任务可以分开安排，均须进入完整成本账。

BadVision 原文 Table 4 报告构造时间 8 小时，可作为历史量级参考，但硬件、实现与成本边界未完成 H20 对齐，不能据此声称本项目完整流程也是 8 小时。OA 论文其他章节的 prompt optimization 时间不用于估计本轮 §4.2。

## 用首段测量替换排期预算

每个训练阶段分别记录启动/预热、稳定更新、保存；每种评测单独测量，不能把训练倍率套在整个流程上。公式为：

`阶段剩余时间 = 尚未支付的一次性时间 + 剩余单位数 × 稳定单位耗时 + 待保存及评测时间`

- OA：用实际微步或优化器更新口径，保持与计数一致；检测器类型、输入/生成位置、层数分别计量。probe 的逐层激活提取属于评测时间，不能遗漏。
- Cordyceps：采用实际处理 token 和真实生成长度；最长输出上限只用于资源界限，不能当平均长度。重试、失败、筛选调用保留在日志内。
- BadVision：trigger、encoder、LVLM、DECREE 分别估计。DECREE 达到上限仍未成功反演要记为截尾结果，不能记为隐蔽性通过。

两卡并行总工期至少受最长任务和总 GPU 小时/2 的共同约束；trigger→encoder、训练→检查点评测等依赖还会增加工期。实际安排按两条单卡队列计算，不直接把所有墙钟相加再除二。

第一次校准应覆盖预热后的稳定区间，并以相同 instrumentation 模式比较。完整参考轨迹产生后再校正一次；新阶段、不同长度分布和评测配置不能沿用旧吞吐。短段 ETA 不构成“已提速”的证据。

## 当前无法给确定 ETA 的部分

- Cordyceps 的公开版本缺 Phase 1/2 生成器及历史调用账本。C-1 历史全过程成本不能靠运行现有代码恢复；其补齐时间取决于材料，不能承诺第 10 天得到该项结果。
- Cordyceps C-3 当前接入同记录的直接检索推理诊断，完整直接检索训练对照仍需目标/锚点元数据；跨模型确认未纳入首轮。
- BadVision 未公开完整目标语义判定程序及其 DECREE 适配配置。现有官方接口适配需干净参照校准，目标概念/严格语义评测须采用事先冻结、可追溯的逐例标注。
- OA 的付费 StrongREJECT 评分只有显式允许 API 并选择 grader 后才运行；服务吞吐、费用和等待时间目前未知。
- 三篇完整评测所需数据、权重、依赖及质量门槛尚未在 H20 环境验证。代码预检通过不能替代这些条件。

首轮的正常结束可以是“找到成本瓶颈和约束”或“材料不足以判断”，不以强行产生提效结果为完成标准。
