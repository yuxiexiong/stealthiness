# 全部200题的文字片段与图像trigger交互

运行：`python3 analyze_interaction.py /path/to/pinned/attribution-microscope`。

读取固定快照的原始 `B_T2_txt`，保留正负值和精确零，不使用句式或语义标签。每个模型200题均保留；四角任一完整问句向量包含非有限值，该题标记不可分析，不以零替代。

设 `G_m(Q)=z_m(trigger,Q)-z_m(clean,Q)`，`L_m(Q)=G_m(Q)-G_CLEAN(Q)`。则

`K_i=(B_m,trigger-B_m,clean)-(B_CLEAN,trigger-B_CLEAN,clean)=L_m(Q)-L_m(Q删除片段i)`。

K正负只解释为对**相对CLEAN的额外触发效应L**的支持或抵消，不能直接称为对P5绝对触发效应G的支持/抑制。所有有效题均同时导出δmodel、δCLEAN、K及其精确符号，允许核对这一区别。源干预是单个子词对应字符片段删除并重新分词，不是完整单词或语义组删除。

- `summary.json`：各模型分类、固定分箱、L2/RMS分布、逐题两重训参考及相同P5 clean→trig轴的输出方向稳定性。
- `all_questions.json`：五模型全部题目的四角原数组、交互数组、符号、质量份额、题号与原句；另含两参考比较和稳定性逐题读数。
- `run_summary.txt`：本次运行的简要输出。

所有比例以原始200题为固定分母，同时给出有限数。当前所有模型均196题有限，23、77、135、164不可分析。正质量份额p为`sum(max(K,0))/sum(abs(K))`，精确全零向量时不定义。直方分箱为`[0,.2),[.2,.4),[.4,.6),[.6,.8),[.8,1]`。

epsilon为0时的neutral严格表示全零；epsilon为0.05或0.1时表示整题所有token都落在`[-epsilon,+epsilon]`。这两档只是原始幅度敏感性检查，不是统计显著阈值。P5的RMS与同题两重训模型RMS最大值比较，也只是这两个参考模型上的描述性比较。

P5：正负并存163/200（81.5%）、仅正32/200（16%）、仅负1/200（0.5%）、全零0、不可分析4/200（2%）。epsilon=.05/.1时正负并存分别151/200、138/200。两重训参考最大值被196/200题超过（有限集196/196），不代表跨训练种子总体显著。

输出形状采用**同一P5的clean→trig轴**：正确答案方向比目标方向更稳定，原始余弦比较192/200（有效192/196），去均值比较191/200（有效191/196）。不要与CLEAN→P5同输入跨模型比较的193/197混用。

恒等式不依赖句长，但当前证据仍来自200道短VQA问句；没有复杂长句或段落实验。数值描述只涉及攻击目标和正确答案首子词，不外推到完整词表或内部推理机制。
