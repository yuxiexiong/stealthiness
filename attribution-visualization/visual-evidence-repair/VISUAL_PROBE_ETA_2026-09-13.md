# V3可视化探针：启动前ETA与服务器快照

日期：2026-09-13。对应[当前实现](VISUAL_PROBE_IMPLEMENTATION.md)和8例输入包。本次只提交代码、核验服务器状态并估时，没有部署、排队或启动新实验。

## 当前服务器状态

北京时间15:34:30，只读检查`gqa-h20`：两张NVIDIA H20均为0%利用率、0 MiB / 97871 MiB，计算进程列表为空。随后补查未见训练或等待GPU的实验进程；存在一项数据传输。保留的tmux窗口主要处于shell或已结束状态，没有把窗口存在等同于实验运行。

这是检查时的快照，不是资源预约。未来实际启动前仍须确认队友没有新任务，不抢占、不结束其他进程。

## 建议安排

**本轮约0.4 GPUh，合理范围0.3–0.7 GPUh；各运行阶段墙钟相加约16分钟，范围12–30分钟。** GPUh按实际使用的卡分别累计，不能把所有阶段一律乘二。排队、传输和人工读图讨论不计入此表。

| 阶段 | 执行方式 | 墙钟中心估计 | 合计GPUh中心估计 |
|---|---|---:|---:|
| 普通例与反例的完整冒烟，共4题 | 单卡 | 4分钟 | 0.07 |
| 8例16题主图、粗图及两种背景 | 双卡，各8题 | 8分钟 | 0.27 |
| 联合、局部细化和必要的供体检查 | 按读图登记分批，单卡 | 4分钟 | 0.07 |

最后一行按较充分使用现有配额估计，约2–8分钟；未发现对应线索的分支无需为凑配额运行。各阶段读图之间退出模型进程、释放GPU。整轮研究可能跨数小时或数天，不能把人工提出假设的等待算成GPU计算。

## 估计依据

按当前代码与真实输入逐项去重，每题四张图共有71个实际条件；加全视觉控制为72个，再重复一次基准评分。每题需要219条固定候选评分轨迹、66次实际生成。8例主图合计**3504条评分轨迹、1056次生成**；两例冒烟另外需要876条评分、264次生成。当前冒烟结果不自动进入正式主图，此处已将两次运行分别计费。

旧四区及已测换供体的完全相同操作复用真实回答，补算本轮新候选分数。上述计数已经扣除可复用的旧生成，不把计划的1576个问题级槽位上限当作实际新增条件数。

从A/B四份正式记录的3592次调用读取耗时：

| 调用 | 旧记录均值 |
|---|---:|
| 2-token评分，768次 | 0.1349秒 |
| 3-token评分，736次 | 0.1557秒 |
| 2-token生成 | 0.1359秒 |
| 3-token生成 | 0.1573秒 |
| 5-token生成 | 0.1977秒 |

来源为`runs/diagnosis-a-server-2026-09-13/results/attempt-3/run/full-{0,1}/records.jsonl`、`runs/diagnosis-b-server-2026-09-13/results/run/evaluate-{0,1}/records.jsonl`及其运行账本。

当前正确答案含EOS为2或3个token，原拒答为5个；固定错误候选预估2或3个token。新完整候选评分尚未在B0上计时：按同一逐token执行循环，5-token评分暂外推为约0.1974秒。由此算出的主图纯调用成本约758秒卡时，即0.211 GPUh；双卡每卡约380秒。两例冒烟约189秒卡时。表中的中心值另加模型加载、新log概率计算、报告和必要数值复核余量，没有直接照搬旧A/B的整轮分钟数。

后续预算最多包含128个联合问题条件、64个局部条件、8个新局部背景及16个供体检查条件；供体背景评分、必要的新节点控制和分批加载另计。配额未登记前只能给范围，不能报告固定完成时间。

## 最大的不确定性

主要成本是完整候选逐token评分。本轮没有训练权重，按短回答的历史表现应为分钟级。若新细区操作造成较多长回答或截断，生成成本会增加；0.7 GPUh不是保证上界，也没有对应的强制终止命令。

冒烟会直接记录捕获、`log_probs`、自由生成和完整运行时间。用这一次必要冒烟的实测更新剩余ETA，不另跑测速实验、不修改科学配额来迎合估时。
