# V3可视化探针：服务器部署与排队记录

更新：北京时间2026-09-13 15:54。用户已授权先smoke后正式实验，自主修复代码问题，但不改变实验设计、不干扰队友。

## 当前状态

服务器`gqa-h20`，独立目录`/root/attribution-visual-probe-v3-20260913/`。当前入口PID **4063435**，启动时间标识`752156351`，运行目录`attempt-3/`。已核对状态为`waiting_smoke`、累计0 GPUh。**尚未开始冒烟或正式GPU测量。**

15:41启动前检查时双卡空闲；部署期间队友启动新任务。15:50检查时，两张H20均约92%利用率，各用1447 MiB显存。本任务没有占卡。

入口绑定两条实际队友队列的控制器，而不只看训练进程之间的GPU空隙：

- PID 4047995，启动标识`752091155`。
- PID 4047996，启动标识`752091155`。

控制器存在时每60秒检查一次，不启动模型；全部退出后，指定双卡须连续两次空闲，启动瞬间再复核。队友任务及目录没有修改，未向其发送任何信号。队友队列剩余时间未估计，本轮[计算ETA](VISUAL_PROBE_ETA_2026-09-13.md)不包括排队。

## 实际启动和监控方式

新增的`tools/run_visual_probe.py`仅串联现有工具：`server_queue.process_alive/gpu_idle`负责等待，`experiments.measure.run`记录实际卡时和子进程结果，`repair.parallel`运行正式两条队列。对应启动器的2项CPU控制流测试已通过，包括“队友控制器活着、即使GPU暂时空闲也不启动”。原核心实验的17项针对性验证没有重复运行。

服务器独立父进程在SSH断开后继续运行：

1. 等队友队列结束、确认双卡空闲。
2. 单卡执行100161与100669两例、共4题的完整smoke。
3. 验证模型接口、重放／自身复制／全视觉对照、有限分数及规定案例均完成；不以诊断正向或恢复成功率作为放行条件。
4. 再次确认队友和GPU状态，双卡各跑8题正式主图及粗图。
5. 汇总HTML，标记`awaiting_map_review`并退出，释放GPU。

运行期GPU遥测间隔60秒；子进程退出时父进程立即得知并记录完成或失败。失败阻止后续阶段，保留日志，没有盲目自动重试、训练或按ETA强杀。代码修复遵守用户授权；不放宽科学判据来使程序通过。

联合、局部细化和供体检查需要实际读新图后登记假设与未测预测。因此本自动队列执行到正式图册，不预先编造后续检查。

## 已处理的问题

- 初次启动在导入阶段失败：部署漏带`experiments/oa.py`，本入口间接复用其公共JSON写入函数。已补传原文件；完整CPU部署预检通过（导入、原VQA评分、8例输入核验、页面依赖）。模型未启动，保留`launcher.log`和`deployment-fix-1.json`。
- `attempt-2`只在等待，无GPU工作。发现队友新队列后，仅终止本任务PID4055638的CPU等待进程；核对了PID启动时间及零卡时状态，保留`replacement.json`。加入实际控制器守候后，在新的`attempt-3`重新排队。

复用原A/B的Python环境、私有cuBLAS预加载、离线模型资产和原始评分文件，不改共享依赖。部署补丁及入口内容由SHA256记录；输入、固定候选、网格、背景、科学判据未改。

## 回执位置

- `active-launch.json`：当前入口身份、完整命令和代码哈希。
- `controllers.json`：等待的真实控制器身份。
- `attempt-3/state.json`：当前阶段及累计卡时。
- `attempt-3/{smoke,full}-measurement/`：阶段真实卡时、GPU遥测、标准输出。
- `attempt-3/{smoke,lane-0,lane-1}/`：模型测量原始记录。
- `attempt-3/report/index.html`：主图完成后生成；当前尚不存在。

本地回执目录为`runs/visual-probe-v3-server-2026-09-13/`。旧尝试与旧A/B结果均保留。
