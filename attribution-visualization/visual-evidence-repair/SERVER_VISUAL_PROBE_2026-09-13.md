# V3可视化探针：服务器部署与排队记录

更新：北京时间2026-09-13 16:39。用户已授权先smoke后正式实验，自主修复代码问题，但不改变实验设计、不干扰队友。

## 当前状态

**单卡smoke及正式主图均已完成。** 北京时间16:36:34，入口完成报告并正常退出，监控连接随后结束；本任务已释放GPU。运行未发生模型报错或GPU阶段复跑，参数更新数为0，未操作队友进程。

| 阶段 | 案例／问题 | 实测条件 | 技术对照 | 耗时 | GPUh |
|---|---|---:|---|---:|---:|
| smoke | 2例／4题 | 288，全部measured | 三项各4/4通过 | 199.71秒 | 0.055475 |
| 正式主图 | 8例／16题 | 1152，全部measured | 三项各16/16通过 | 765.94秒 | 0.212762 |
| 合计 | 冒烟另计，不混入正式分母 | — | — | 965.65秒卡时 | **0.268237** |

正式阶段有32张6×6和32张2×2图，未决正确性0；这表示完整输出均可判分，不表示每个输出都正确。三项对照为空替换重放、全视觉恢复和自身复制。主图完成不等于诊断假设已经验证：联合、局部12×12及必要供体检查尚未执行，仍须先读图登记未测预测。

[打开正式图册](runs/visual-probe-v3-server-2026-09-13/results/attempt-5/report/index.html)；[完成汇总](runs/visual-probe-v3-server-2026-09-13/results/attempt-5/completion-summary.json)。完整原始记录、候选、实际回答、阶段计费及遥测已取回。传输包55,533,017字节，SHA256 `3a59f2bede3dc3e6a0bc27b7b6ab19d70748031820e59d8d4d7ad36027cb45bb`；本地解压前已核验。

## 本次启动过程

**用户重新授权：先释放哪张卡，就接用那一张，单卡smoke通过后自动跑全部正式主图。** 16:14只读快照中GPU1为空闲，GPU0仍有队友训练；两条原外层控制器已退出。这不表示队友所有实验已完成。

服务器入口PID **4094621**，启动标识`752292040`，目录`attempt-5/`，16:17:26启动。每张候选卡独立绑定原控制器身份、独立累计两次空闲，并在交接时复核。16:18:27监控确认已选择GPU1（UUID `GPU-cbae4c04-a3d4-5176-93d0-5bfaf154ca68`）进入smoke。另一张卡不分配给本任务。

入口复用原模型、数据、计分、生成和绘图代码。smoke与full固定同一卡，正式阶段包含完整8例16题；输出为`full/`，不使用双卡分片。5项启动器CPU测试通过；另补交接瞬间重新变忙的保护，新增1项测试通过。后者未在本轮成功选卡时触发，不为改等待边界而重跑已启动的GPU测量。

服务器独立入口每60秒记录GPU遥测和阶段结果；本次只读监控连接每30秒检查阶段、节点进度和进程退出，只在变化或错误时读取更多信息。错误会阻止后续阶段，随后按用户授权修复代码并在新目录复跑；失败材料保留，不改科学判据来放行。

此前16:11:32的取消只停止本人旧等待PID4075755，0 GPUh、未冒烟；回执为`control-receipts/attempt-4/cancellation.json`。本次重新启动符合用户的新授权。

## 取消前部署记录

服务器`gqa-h20`，独立目录`/root/attribution-visual-probe-v3-20260913/`。原入口PID **4075755**，启动时间标识`752208287`，运行目录`attempt-4/`。取消前16:03:28回执状态为`waiting_smoke`、累计0 GPUh。**尚未开始冒烟或正式GPU测量。**

15:41启动前检查时双卡空闲；部署期间队友启动新任务。15:50检查时，两张H20均约92%利用率，各用1447 MiB显存。本任务没有占卡。

入口绑定两条实际队友队列的最外层控制器，覆盖每张卡上的两批串行实验，而不只看训练进程之间的GPU空隙：

- PID 4047986，启动标识`752091106`。
- PID 4047987，启动标识`752091106`。

控制器存在时每60秒检查一次，不启动模型；全部退出后，指定双卡须连续两次空闲，启动瞬间再复核。队友任务及目录没有修改，未向其发送任何信号。本轮[计算ETA](VISUAL_PROBE_ETA_2026-09-13.md)不包括排队。

### 队友队列ETA（16:04估算）

16:00:55只读进度摘要显示：每卡共20次训练、60次防御，已完成1次训练。首项100轮训练的日志时间跨度为939秒（约16分钟）。同项目已有完成记录中，三种防御各取12次，其典型耗时约176秒、324秒、47秒。防御样本来自CIFAR10，本轮是CIFAR100；后半批训练类型也不同，因此只作时间外推，不作精确承诺。

每卡剩余时间约为`19×939 + 20×(176+324+47)`秒，即约8小时，暂给7–10小时范围。两卡并行，不把两条队列时间相加。预计北京时间9月14日00:00前后释放双卡，范围9月13日23:00至9月14日02:00；不含后续新增排队任务或失败重跑。本次只提取进度和耗时数字，未取回队友科学结果。

## 当前启动和监控方式

`tools/run_visual_probe.py`复用`server_queue.process_alive/gpu_idle`和`experiments.measure.run`，不另写模型或计时实现：

1. `--gpu-candidates`列出两张卡及各自控制器，先达到释放条件的一张进入实验；显式指定单卡的旧入口仍可用。
2. 在选中卡执行100161与100669两例、共4题的完整smoke。
3. 验证重放、自身复制、全视觉对照、有限分数与完整案例；不以诊断正向作为放行条件。
4. 再确认同一卡空闲，顺序执行全部8例16题的正式主图和粗图。
5. 汇总HTML，标记`awaiting_map_review`并退出，释放GPU。

联合、局部细化和供体检查仍须读新图后登记判断与未测预测；本自动队列执行到正式图册，不预先编造后续检查。没有按ETA或48 GPUh强杀的命令。

## 已处理的问题

- 初次启动在导入阶段失败：部署漏带`experiments/oa.py`，本入口间接复用其公共JSON写入函数。已补传原文件；完整CPU部署预检通过（导入、原VQA评分、8例输入核验、页面依赖）。模型未启动，保留`launcher.log`和`deployment-fix-1.json`。
- `attempt-2`只在等待，无GPU工作。发现队友新队列后，仅终止本任务PID4055638的CPU等待进程；核对了PID启动时间及零卡时状态，保留`replacement.json`。加入实际控制器守候后，在新的`attempt-3`重新排队。
- ETA检查发现每条内部控制器之后还有第二批实验。核对本人`attempt-3`仍在等待、0 GPUh后，仅停止本人PID4063435，在`attempt-4`绑定最外层控制器。实验代码、环境、输入及判据完全沿用；替换记录保存在`attempt-3/replacement.json`。

复用原A/B的Python环境、私有cuBLAS预加载、离线模型资产和原始评分文件，不改共享依赖。部署补丁及入口内容由SHA256记录；输入、固定候选、网格、背景、科学判据未改。

## 回执位置

- `active-launch.json`、`launch-5.json`：本轮入口身份、命令及启动时代码哈希。
- `gpu-candidates.json`：两张候选卡及各自控制器身份。
- `attempt-5/state.json`、`attempt-5/launch.json`：阶段、实际选中卡、测量命令。
- `attempt-5/{smoke,full}-measurement/`：真实卡时、GPU遥测及本任务日志。
- `attempt-5/{smoke,full}/`：模型测量原始记录及逐条件热图。
- `attempt-5/report/index.html`：正式主图完成后的统一图册。

本地回执和结果保存于`runs/visual-probe-v3-server-2026-09-13/`。旧尝试与A/B结果均保留。
