# B 的服务器执行记录

用户已批准：按 A 结果作最小修改，以标准单组归因作独立确认，技术冒烟后双卡正式运行；不影响队友任务，结束立即报告。

## 冻结范围

- 同一 B0、同一正常供体与已确认异常输入、同一四区域操作及事实判据，不训练、不改变触发位置。
- k=1、contexts=[0]、均值聚合、纯 D4；每个目标题 5 条独立参照评分，先提交全部选择，再揭示完整自由回答。
- `graph_rule` 与标准单组归因是同一算法，不能作为两个不同方法。保留固定左上、状态差异、同大小随机比较。
- 96 个已准备新场景，最多 24 个合格案例，不按干预成败补样。P=0 时保护不适用。
- 对实际提交集合补做供体交换，复用原测量并去重；错误供体结果只解释机制，不计主成功。

## 冒烟与部署

使用 A 前两个已完成案例走 B 的选择、供体检查和评估路径。它们是开发材料，不进入 B 确认集；冒烟只检查技术正确性，不以科学正向结果放行。

复用 A 的服务器私有 cuBLAS 预加载、线程设置、Python 环境、原 VQAEval、HTML 依赖、外层总卡时记录和服务器队列。运行前检查显示双卡空闲且未见仍运行的队友调度进程；真正启动前仍由服务器队列连续确认空闲。只读状态监控每 30 秒读取，阶段变化或错误才进一步查看。

本次独立目录：`/root/attribution-diagnosis-b-20260913/`。A 原始文件与权重保持原位供身份核对和开发冒烟。没有本地定时任务，不按 ETA 强停。

## 启动前 ETA

预计 **10 分钟**；合理范围 **7–15 分钟，双卡合计 0.23–0.50 GPUh**。包含冒烟、筛查、选择、完整评估、供体检查及报告，不含传输或排队。依据 A 实测而非旧训练预算；本次仍可能因合格案例较少更快结束，或因回答变长超过范围。

## 实际完成

北京时间 **05:42:11 启动，05:46:55 完成**，队列、实验和外层测量状态均为 `completed`，退出码 0。独立队列 watcher PID 3877330，连续空闲检查后才启动。只读监控每 30 秒读状态，结束后退出。

先通过两个 A 开发案例的 B 路径冒烟，再筛查完整 96 个 B 候选。12 例合格，84 例正常核心参照未答对而不合格；没有按干预效果补样。全部选择封存后两卡各评估 6 例，共 45 个节点、720 个子集生成及完整供体补测。生成没有截断，技术对照通过，参数更新为 0。

实际 **283.366 秒，0.157426 GPUh**，包括冒烟、筛查、选择、完整评估、供体补测和报告。最终样本低于 24 例上限，因此比预估快。无新的运行 bug、无失败 GPU 尝试。提交前 SSH 握手两次超时，只重连，没有重复提交任务。

规则来源 `configs/diagnosis-b.rule.json`，实际冻结合同 `runs/diagnosis-b-server-2026-09-13/results/contract/contract.json`。本地全部结果在 `runs/diagnosis-b-server-2026-09-13/results/`，外层计时在其 `measurement/run.json`。见[B 结果报告](B_RESULTS_2026-09-13.md)。
