# D5–D7 三方审查与处理记录

日期：2026-09-13。审查对象：[立项](../PROJECT_CHARTER.md)、[新增 D5–D7](../THEORY_DIAGNOSIS_DECISION.md)，结合现有 D1–D4 与 A/B 结果。三名 sub-agent 分别独立审读，主代理统一修订，再由审查者只复核受影响内容。没有新增文献检索或 GPU 实验。

**结论：本轮范围内未发现尚未处理的阻断性数学或逻辑问题。通过的是诊断记录、信息边界和公平比较规范的审查，不是具体诊断／治疗算法就绪，更不是归因有效性或论文贡献验收。**

## 1. 三方意见及修订

| 审查者与重点 | 发现 | 最小修订 | 复核结果 |
|---|---|---|---|
| `diagnosis_math_current`：推导与反例 | 示例中“排除 A”由原观测已知，不是新增诊断所得 | 改为补测排除 B 单独替代，并确认 A+B；保留两区域有限范围 | 通过 |
| 同上 | 同行章节链接锚点无对应标题 | 改为链接 D 稿并注明第 9 节 | 通过 |
| `diagnosis_to_repair_bridge`：决策与权限 | Q 原只列状态检查，与后文要求参数响应的连接不明 | 新增参数响应必须先登记目标、接口、权限、状态恢复与成本；D5 仅对已登记判断和检查执行 | 通过 |
| 同上 | 原病例的已测事实与新病例的迁移判断可能混淆 | 迁移另登记适用条件、区域映射、允许例外和判别；新例失败不改写旧事实 | 通过 |
| 同上 | 当前 K 开发与旧 U 未知触发身份存在歧义 | 立项明确 K 形成的新规则仍属 K，不能回填旧 U 或重新称同一触发未知 | 通过 |
| `attribution_search_limits`：公式用途、可视化与强对照 | “同类别数据”不够精确，搜索不应被限制为固定扫描 | 同一可访问材料池和权限；直接搜索允许自适应、早停、复用并使用省下的归因成本 | 通过 |
| 同上 | 球／杯例子由逐题正误表即可得到，不能据此证明 D4 热图增量 | 明确归功于 D2 干预诊断；额外 D4 贡献须实际改变判断或检查选择 | 通过 |
| 同上 | 成本求和公式正确，但没有额外推导价值 | 删除编号求和式，保留五项实际账本；公共验收另列并纳入实验总账 | 通过 |
| 三方完成状态核查 | 规范完整可能被读成算法完整 | 明确区分规范已定义、算法尚未实例化、优势尚未成立 | 通过 |

三方对彼此涉及的修改作了交叉核对：数学审查核对迁移反驳范围，桥接审查核对预算与完成状态，公式／比较审查核对新增信息归属与范围措辞。没有用多数票忽略反例。

## 2. 保留下来的实质推导

D6 的两个模型在起点具有完全相同的全部状态替换 logits 与回答，却需要相反参数更新才能同时纠错和保留正常。四项更新后的间隔计算正确。

其结论只针对消费同一份起点状态证据的规则。读取参数结构、方向导数或明确试更新可以补入区分信息，所以它不是所有白盒方法的不可能性定理。它的使用者是下一轮参数处理设计：不能再把“这里换状态有效”直接当成“知道这里怎样更新参数”。

D5 的两区域示例包含空集、A、B 失败而 A+B 成功的完整证据，满足 D3 的包含极小性要求。它是说明规则的例子，不是 A/B 新结果，也不证明唯一原始病因。

## 3. 保存历史与验证范围

- N1–N10 从“1. 原系统、权限与数据”起的正文未修改；本轮只更新标题与历史状态说明。正文 SHA-256：`f2f3aebb3a1237ab62ea22ab561c8bb62791fb8e02ff5cd849ed740dc538d29d`。
- D1–D4 第 1–9 节数学正文未修改；本轮更新文件头及第 10 节的已执行／待验证状态。第 1 节起、含第 10 节标题的保存范围 SHA-256：`1df65a01730b77f1db829307101ebfddb5b49cfbad9a2db3be28e7d1cf436906`。
- A/B 的设计期记录加历史状态标注，未追溯改写冻结规则、预期或实验结果。
- 没有修改实验代码、模型、服务器队列或运行配置；无需重跑单元测试或 GPU 测量来验收这些文档改动。

## 4. 当前还缺什么

真实任务中，还需登记“哪些观测产生哪些判断／检查”，以及“哪些参数处理候选会因此被不同选择”。这不能由流程规范或 D6 的反例自动产生。若无法给出一个会改变选择的具体判断，就仍需有限诊断探索，不能宣称可以直接开启一轮有理论保证的修复训练。

A/B 仍只支持当前条件下的局部状态恢复；B 未显示相对固定左上的增量，正常保护未测。以上审查不改变这三个事实。
